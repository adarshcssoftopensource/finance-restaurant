# Samples

| Name | Description | Path |
|------|-------------|------|
| Basic Routing | Define routes in `app/routes.ts` using `route`, `index`, `layout`, and `prefix` helpers. | [basic-routing.md](./basic-routing.md) |
| Data Loading | Load route data server-side with `loader` and access it via `loaderData` in the component. | [data-loading.md](./data-loading.md) |
| Error Boundary | Catch loader, action, and render errors with an `ErrorBoundary` export in a route module. | [error-boundary.md](./error-boundary.md) |
| Form Action | Submit a form with `<Form method="post">` and handle the mutation in a server `action`. | [form-action.md](./form-action.md) |
| Form Validation | Validate server-side in an `action` and return field errors without navigating away using… | [form-validation.md](./form-validation.md) |
| Middleware Auth | Protect routes with reusable authentication middleware using `createContext` and the… | [middleware-auth.md](./middleware-auth.md) |
| Navigation Blocking | Prevent accidental navigation when a form has unsaved changes using `useBlocker`. | [navigation-blocking.md](./navigation-blocking.md) |
| Pending UI | Show loading indicators during navigation and form submission using `useNavigation` and… | [pending-ui.md](./pending-ui.md) |
| Pre-Rendering | Generate static HTML at build time for performance-critical pages using the `prerender`… | [pre-rendering.md](./pre-rendering.md) |
| Search Params | Read and update URL search parameters with `useSearchParams` without triggering a full… | [search-params.md](./search-params.md) |
| Session Auth | Implement cookie-based session authentication with `createCookieSessionStorage`. | [session-auth.md](./session-auth.md) |
